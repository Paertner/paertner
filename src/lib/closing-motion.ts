// Include landscape tablets and touch devices, not only narrow phone screens.
export const SIMPLE_CLOSING_QUERY =
  "(max-width: 1200px), (hover: none), (pointer: coarse), (prefers-reduced-motion: reduce)";
